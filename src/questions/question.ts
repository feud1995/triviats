export default class Question {
  constructor(readonly id: number, readonly text: string) {
    // this._id = id;
    // this._text = text;
    // this._answers = Array<Answer>();
  }

  // private _id: number;
  // get id() : number { return this._id; }
  // set id(value: number) { this._id = value; }

  // private _text: string;
  // get text(): string { return this._text; }
  // set text(value: string) { this._text = value;}
  
  // private _answers: Array<Answer> = new Array<Answer>();
  // get answers(): Array<Answer> { return this._answers; }
  // set answers(value: Array<Answer>) { this._answers = value; }
}